app-id: ai.whisperwayland.Dictation
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
command: whisper-wayland
build-options:
  build-args:
    - --share=network
finish-args:
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --device=all # Required for evdev hotkeys (not ideal for Flathub, but necessary for current architecture)
  - --talk-name=org.freedesktop.portal.Desktop
  - --talk-name=org.freedesktop.Notifications
  - --own-name=ai.whisperwayland.Dictation

modules:
  - name: portaudio
    buildsystem: autotools
    sources:
      - type: archive
        url: http://files.portaudio.com/archives/pa_stable_v190700_20210406.tgz
        sha256: 47efbf42c77c19a05d22e627d42873e991ec0c1357219c0d74ce6a2948cb2def

  - name: python3-requirements
    buildsystem: simple
    build-commands:
      - pip3 install --prefix=/app --ignore-installed -r requirements.txt
    sources:
      - type: file
        path: requirements.txt

  - name: whisper-wayland
    buildsystem: simple
    build-commands:
      - mkdir -p /app/bin
      - mkdir -p /app/share/whisper-wayland
      - cp -r src /app/share/whisper-wayland/
      - cp -r assets /app/share/whisper-wayland/
      - echo '#!/bin/sh' > /app/bin/whisper-wayland
      - echo 'export PYTHONPATH=/app/share/whisper-wayland/src:$PYTHONPATH' >> /app/bin/whisper-wayland
      - echo 'exec python3 /app/share/whisper-wayland/src/main.py "$@"' >> /app/bin/whisper-wayland
      - chmod +x /app/bin/whisper-wayland
      - install -D whisper-wayland.desktop /app/share/applications/ai.whisperwayland.Dictation.desktop
    sources:
      - type: dir
        path: .
