{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Default capability set for VoxCtr windows",
  "platforms": ["linux", "windows", "macos"],
  "windows": ["settings", "overlay", "history"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "notification:default",
    "fs:default",
    "dialog:default"
  ]
}
